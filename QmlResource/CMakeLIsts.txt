set(MODULE_NAME QmlResource)
set(LIB_NAME ${MODULE_NAME}Lib)

qt_add_library(${LIB_NAME} STATIC)

set_target_properties(${LIB_NAME} PROPERTIES AUTOMOC ON)
target_link_libraries(${LIB_NAME} PRIVATE Qt6::Quick)

qt_add_qml_module(${LIB_NAME}
    URI ${MODULE_NAME}
    # VERSION 1.0
    RESOURCE_PREFIX /
    QML_FILES
        Main.qml
        PersonaliaPage.qml
        DiaryPage.qml
        FoodDefinitionList.qml
        FoodDefinitionListDelegate.qml
        FoodDefinitionModel.qml
        FoodDialog.qml
        ItemsPage.qml
        ManualDialog.qml
        RecipeDialog.qml
        RecipePage.qml
        controls/Button.qml
        controls/CheckBox.qml
        controls/ComboBox.qml
        controls/Dialog.qml
        controls/GroupBox.qml
        controls/Label.qml
        controls/ListView.qml
        controls/RadioButton.qml
        controls/RowLayout.qml
        controls/TabButton.qml
        controls/TextField.qml
)
